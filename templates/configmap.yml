apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.configmap.name }}
  namespace: {{ .Values.namespace | default .Release.Namespace }}
data:
  AGE_THRESHOLD_DAYS: {{ .Values.configmap.ageThresholdDays }}
